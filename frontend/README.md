# EchoMind frontend

阶段 5 的 React、TypeScript 和 Vite 前端。提供 `/`、`/import`、`/imports/:sourceFileId`、`/conversations` 和 `/conversations/:conversationId`；请求缓存仅存在内存，不使用浏览器持久化存储。

安装、启动和检查命令见仓库根目录的 `README.md`。
