# Samples

此目录只允许提交合成或充分脱敏的聊天样本。

- 不得包含真实姓名、账号、电话、邮箱、地址、密钥、健康信息或可重新识别的组合信息。
- 每个样本应说明格式、用途、预期解析结果和是否完全合成。
- 真实 WeFlow 格式必须由用户授权并完成脱敏后才能加入。

`synthetic/` 中的阶段 3 文件全部由人工构造，名称仅使用 Person A/Person B，正文均明确包含 Synthetic。当前包括 JSON、CSV、固定纯文本格式的有效样本和各格式无效样本；它们不代表任何第三方平台的真实导出结构。
